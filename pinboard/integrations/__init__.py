"""Optional third-party integrations.

Each module here imports its dependency lazily and reports a friendly
error if the dependency is not installed. Install via the matching
extra: ``pip install pinboard[langfuse]``.
"""
